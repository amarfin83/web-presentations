# Presentation Folder Template

Copy this shape when adding a new presentation:

```text
<project-slug>/<presentation-slug>/
  index.html
  assets/
    README.md
```

The public URL will be:

```text
https://amarfin83.github.io/web-presentations/<project-slug>/<presentation-slug>/
```
